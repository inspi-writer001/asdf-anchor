#!/usr/bin/env bash
set -euo pipefail

# FIXED: Use coral-xyz/anchor instead of solana-foundation/anchor
GH_REPO="https://github.com/coral-xyz/anchor"

latest_version=$(curl -sI "${GH_REPO}/releases/latest" |
  grep -i location |
  sed -E 's|.*/tag/v?([^/]+)\s*|\1|')

echo "$latest_version"