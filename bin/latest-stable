#!/usr/bin/env bash
set -euo pipefail

GH_REPO="https://github.com/solana-foundation/anchor"

latest_version=$(curl -sI "${GH_REPO}/releases/latest" |
  grep -i location |
  sed -E 's|.*/tag/v?([^/]+)\s*|\1|')

echo "$latest_version"
