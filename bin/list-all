#!/usr/bin/env bash
set -euo pipefail

# FIXED: Use coral-xyz/anchor instead of solana-foundation/anchor
curl -s "https://api.github.com/repos/coral-xyz/anchor/releases" |
  grep '"tag_name":' |
  sed -E 's/.*"v?([^"]+)".*/\1/' |
  sort -u